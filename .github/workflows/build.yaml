name: build, test and deploy
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  SERVER_URL: $SERVER_URL
  DOCKER_REGISTRY: $DOCKER_REGISTRY
  DOCKER_USERNAME: $DOCKER_USERNAME
  DOCKER_PASSWORD: $DOCKER_PASSWORD
  CODECOV_TOKEN: $CODECOV_TOKEN
  NAME: cashbook

jobs:
  build-frontend:
    name: build client
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: checkout
        uses: actions/checkout@v4
                
      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: install pnpm
        run: npm install -g pnpm

      - name: replace server url
        run: sed -i "s|SERVER_URL|$SERVER_URL|g" ./client/src/environments/environment.ts

      - run: pnpm install
        working-directory: ./client

      - run: pnpm build
        working-directory: ./client
        
      - run: pnpm lint
        working-directory: ./client
        
      # - run: pnpm pw:install
      #   working-directory: ./client

      # - run: pnpm pw
      #   working-directory: ./client

      # - uses: actions/upload-artifact@v3
      #   if: always()
      #   with:
      #     name: playwright-report
      #     path: playwright-report
      #     retention-days: 10
      #   working-directory: ./client

      - name: set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: docker metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: $DOCKER_REGISTRY/$NAME-client

      - name: log in to Docker Registry
        uses: docker/login-action@v2
        with:
          registry: $DOCKER_REGISTRY
          username: $DOCKER_USERNAME
          password: $DOCKER_PASSWORD

      - name: build and push
        uses: docker/build-push-action@v6
        with:
          context: client
          push: true
          tags: $DOCKER_REGISTRY/$NAME-client:latest
          platforms: linux/arm64

  build-backend:
    name: build server
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.x

      - name: dotnet restore
        run: dotnet restore

      - name: dotnet test
        run: |
          export ConnectionStrings__AppDbContext="Server=localhost;Port=5432;Database=test;User Id=test;Password=test;"
          export Jwt__Key="ThisIsMySuperSecretKeyWhichNeedsToBeASpecificLengthForSomeReason"
          export Jwt__Issuer="test.de"
          export Jwt__Audience="test.de"
          dotnet test --coverage --coverage-output-format cobertura --report-trx --results-directory ./TestResults

      - name: install trx2junit
        run: dotnet tool install --global trx2junit

      - name: run trx2junit
        run: trx2junit --output TestResults/junit TestResults/*.trx

      - name: Upload test results to Codecov
        uses: codecov/codecov-action@v5
        if: ${{ !cancelled()
        with:
          token: $CODECOV_TOKEN
          report_type: test_results
          files: ./TestResults/junit/*.xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        if: ${{ !cancelled()
        with:
          token: $CODECOV_TOKEN
          files: ./TestResults/*.xml

      - name: log in to Docker Registry
        uses: docker/login-action@v2
        with:
          registry: $DOCKER_REGISTRY
          username: $DOCKER_USERNAME
          password: $DOCKER_PASSWORD

      - name: build and push
        uses: docker/build-push-action@v6
        with:
          context: server
          push: true
          tags: $DOCKER_REGISTRY/$NAME-api:latest
          platforms: linux/arm64
